import React from 'react';
import Colors from '../assets/Colors'
import injectSheet from 'react-jss'

const SideMenu = ({ items, show }) => (
  <div>
    <h1>TESTE</h1>
  </div>
);

const styles = {
   
}

export default injectSheet(styles)(SideMenu)